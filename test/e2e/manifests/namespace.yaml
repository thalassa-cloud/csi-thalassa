apiVersion: v1
kind: Namespace
metadata:
  name: ${E2E_NAMESPACE}
  labels:
    # Pod Security Standards privileged
    pod-security.kubernetes.io/enforce: "privileged"
    pod-security.kubernetes.io/audit: "privileged"
